// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://ruedaleatherwallet.github.io',
    base: "rueda_leather_wallet_catalog"
});
